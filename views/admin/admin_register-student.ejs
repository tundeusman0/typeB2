<%- include('../layout/header.ejs') %>

<div class="back-to-pages">
<a href="/admin"><i class="fas fa-arrow-left"></i> Back to Admin</a>
</div>

<form method="POST" action="/admin/register-student" enctype="multipart/form-data">
    <div>
        <label for="">Email</label>
        <input type="text" name="email" placeholder="email@email.com" />
    </div>
    <div>
        <label for="">Matric Number</label>
        <input type="text" name="number" placeholder="id" />
    </div>

    <div>
    <label>Faculty</label>
    <% let message= [] %>
        <select name="faculty" id="faculty">
        <option value="" selected disabled>Select Faculty</option>
        <% if(faculties){ %>
        <% faculties.forEach(faculty=>{ %>
            <option value="<%= faculty.faculty %>"><%= faculty.faculty %></option>
            <% })%>
            <% }%>
        </select>
    </div>
    <div>
        <label for="">Department</label>
        <select name="department" id="department">
            <option value="" selected disabled>Select Department</option>
        <% if(faculties){ %>
            
            <% for(let faculty of faculties){ %>
            <option value="" disabled><%= faculty.faculty %></option>
            <% faculty.department.forEach(department => { %>
                    <option value="<%= department %>"><%= department %></option>
                    <% }) %>
        <% }%>
        <% }%>
        </select>
    </div>
    
    <button>Submit</button>
</form>
<script>
    let gp = document.getElementById("faculty")
    gp.addEventListener("change",()=>{
        let facult = `<%- faculties[0].department%>`
                // console.log(facult)
                let dept=[],headd=[];
            <% for(let faculty of faculties){ %>
             headd.push( faculty.faculty)
            <% faculty.department.forEach(department => { %>
                 dept = department
                    <% }) %>
                    <% } %>
            // console.log(headd)
            // console.log(dept)

        
    })
            
</script>

<div style="bottom:0;">
<%- include('../layout/footer.ejs') %>
</div>