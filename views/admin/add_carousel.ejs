<%- include('../layout/admin_header.ejs') %>

<h2>Add a Carousel</h2>
<a href="/admin/carousel">Back to all Carousel</a>
<br />
<form method="POST" action="/admin/carousel/add-carousel" enctype="multipart/form-data">
    <div>
        <label for="">Title</label>
        <input type="text" name="title" value="<%= title %>" placeholder="title" />
    </div>
    <div>
        <label for="">desc</label>
        <input type="text" name="desc" value="<%= desc %>" placeholder="desc" />
    </div>
    <div>
        <label for="">slug</label>
        <input type="text" name="slug" value="<%= slug %>" placeholder="slug" />
    </div>
    <div>
        <label for="">Image</label>
        <input id="input" type="file" name="image" placeholder="image" onchange="showMyImage(this)"/>

        <img id="output" src="#" alt="">
    </div>
    

    <button>Submit</button>
</form>

<%- include('../layout/footer.ejs') %>

<script>

  function showMyImage(fileInput) {
        var files = fileInput.files;
        for (var i = 0; i < files.length; i++) {           
            var file = files[i];
            var imageType = /image.*/;     
            if (!file.type.match(imageType)) {
                continue;
            }           
            var img=document.getElementById("output");            
            img.file = file;    
            var reader = new FileReader();
            reader.onload = (function(aImg) { 
                return function(e) { 
                    aImg.src = e.target.result;
                    aImg.setAttribute('style',"width:50%;heigth=20%") 
                }; 
            })(img);
            reader.readAsDataURL(file);
        }    
    }

</script>
